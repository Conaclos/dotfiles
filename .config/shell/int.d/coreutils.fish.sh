
alias cd=' cd'

alias dd='dd status=progress'

alias ls=' command ls --color=auto' # Enable colors only where supported
alias lh=' command ls -Alhv --color=always --time-style=long-iso --group-directories-first'
    # Human friendly ls
    # -v: natural sort of number

alias mv='mv -i'
    # Ask before to erase an existing target

